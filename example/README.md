# fb_embedded_example

Demonstrates how to use the fb_embedded plugin. Deploys the Firebird embedded engine, creates a new database using the engine, then inserts a test record and selects it back.

Uses the [fbdb](https://pub.dev/packages/fbdb) package to access the database.
